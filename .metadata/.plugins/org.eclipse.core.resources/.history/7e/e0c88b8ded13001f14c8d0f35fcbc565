package com.kosta.board;

import java.sql.Connection;
import java.sql.PreparedStatement;

public class BoardTest1 {

	
	private Board connection = Board.oracleConn();
	
	public void aa(Board Empvo) {
		String sql = "SELECT * FROM emp";

		try(Connection conn = connection.getConnection();
				PreparedStatement pstmt = conn.prepareStatement(sql)){
			
			/*물음표채워줘야지*/
			pstmt.setString(1, Board.get());
			pstmt.setString(2,  Board.get());
			pstmt.setInt(3,  Board.getPubYear());
			
			if(pstmt.executeUpdate() == 1) {
				System.out.printf("\n### [%s] 영화가 신규 등록 되었습니다.\n", movie.getMovieName());
			} else {
				System.out.println("신규 영화 등록 실패!");
			}
			
		} catch (Exception e) {
		e.printStackTrace();
		}
	}
	}
	
	
	
	
	
	
	
//	public static void main(String[] args) {
//	}

}
